/**
 * Viewport width (px) at or below which the marketing header collapses its
 * center/end nav into the hamburger menu. Kept in one place so the CSS media
 * queries and the JS `MediaQuery` checks stay in sync.
 */
export const HEADER_MOBILE_BREAKPOINT = 992;
